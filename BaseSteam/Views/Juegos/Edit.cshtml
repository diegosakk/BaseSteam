@model BaseSteam.Models.Juego
@{
    ViewData["Title"] = "Edit";
}
@{
    var errorMessage = TempData["ErrorMessage"] as string;
    var successMessage = TempData["SuccessMessage"] as string;
}
<h1>Editar Juego</h1>
<div>
    <form asp-action="Edit" class="col-lg-6 offset-lg-3">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <label class="form-label">Nombre</label>
            <input type="text" class="form-control" asp-for="Nombre" />
        </div>
        <div class="form-group">
            <div class="form-group">
                <label class="form-label">Desarrollador</label>
                <select asp-for="Desarrollador" class="form-select" asp-items="ViewBag.IdDesarrollador"></select>
            </div>
            <label class="form-label">Editor</label>
            <select asp-for="Editor" class="form-select" asp-items="ViewBag.IdEditor"></select>
        </div>
        <div class="form-group">
            <label class="form-label">Categoria</label>
            <select asp-for="Categoria" class="form-select" asp-items="ViewBag.IdCategoria"></select>
        </div>
        <div class="form-group">
            <label class="form-label">Usuario</label>
            <select asp-for="UsuarioRegistrado" class="form-select" asp-items="ViewBag.IdUsuario"></select>
        </div>
        <div class="form-group">
            <label class="form-label">Plataforma</label>
            <input type="text" class="form-control" asp-for="Plataforma" />
        </div>
        <div class="form-group">
            <label class="form-label">Precio</label>
            <input type="text" class="form-control" asp-for="Precio" />
        </div>
        <div class="mt-3 text-end">
            <a asp-action="Index" class="btn btn-outline-info">volver</a>
            <input type="submit" class="btn btn-outline-info" value="Edit" />


        </div>

    </form>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    function confirmarGuardado() {
        var requiredFields = document.getElementsByClassName('required-field');
        var missingFields = [];

        for (var i = 0; i < requiredFields.length; i++) {
            var field = requiredFields[i];
            if (field.value.trim() === '') {
                var label = field.previousElementSibling.textContent;
                missingFields.push(label);
            }
        }

        if (missingFields.length > 0) {
            mostrarAlerta('Los siguientes campos son obligatorios: ' + missingFields.join(', '));
            return false;
        }

        return true;
    }

    function mostrarAlerta(mensaje) {
        Swal.fire({
            icon: 'error',
            title: 'Error',
            text: mensaje,
            confirmButtonText: 'Aceptar'
        });
    }

    function limpiarCampos() {
        $('#Nombre').val('');
        $('#Descripcion').val('');
    }
</script>

@if (!string.IsNullOrEmpty(errorMessage))
{
    <script>
        mostrarAlerta('@errorMessage');
    </script>
}

@if (!string.IsNullOrEmpty(successMessage))
{
    <script>
        Swal.fire({
            icon: 'success',
            title: 'Éxito',
            text: '@successMessage',
            confirmButtonText: 'Aceptar'
        });
    </script>
}
